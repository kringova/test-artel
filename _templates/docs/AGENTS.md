<!-- ШАБЛОН Артели · документ «контракт проекта для агента»
     APC: Audience = агент (любой вендор) / новый контрибьютор
          Purpose (JTBD) = «когда я впервые в этом проекте, хочу за минуту понять
                           правила, команды и конвенции, чтобы работать без объяснений»
     Стайлгайд: открытый AGENTS.md-формат, машиночитаемо, кратко, явные команды/пути.
     Это вендор-нейтральный контракт — его читают Claude Code, Cursor, Codex и др. -->

# AGENTS.md — <Название проекта>

<Одна строка: что это за проект и где корень.>

## Команды

```
<установка>     # поднять зависимости
<запуск>        # дев-режим
<сборка>        # прод-сборка
<тесты/линт>    # проверки
```

## Структура

<ключевые директории и что где лежит — список>

## Конвенции

<стиль кода, именование, паттерны, чего НЕ делать>

## Деплой

<как катится в прод — кратко + ссылка на runbook (`deploy/`/`docs/`). Точка истины,
не гадать. Деплой-конфиг живёт в репо, не в локальном игноре.>
